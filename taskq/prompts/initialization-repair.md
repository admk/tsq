Repair the proposed taskq exploration profile for this objective-generation
prompt:
$objective_prompt

Validation error:
$error

The profile is at $profile_path. Correct `objective.md`, wizard-managed config
values, and files under assets/ as required. Preserve profile metadata, prompt
references, controller settings, and every file under prompts/. Undo any
unauthorized repository or profile change made by the previous attempt, but
make no new change outside the allowed objective, config values, and assets. Do
not execute evaluator code. Recheck the objective, every command, numeric range,
score relationship, asset path, and file constraint against the full
initialization contract above before finishing.
